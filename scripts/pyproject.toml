[project]
name = "piescript-scripts"
version = "0.1.0"
requires-python = ">=3.10"
description = "Design space analysis and report generation for piescript"
dependencies = ["pyyaml>=6.0", "rich>=13.0"]
